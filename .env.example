# Copier en .env (jamais commité) et remplacer les valeurs.
DB_PASSWORD=change-moi
# Générer une clé : openssl rand -base64 32
JWT_SECRET=
# true dès que le site est servi en HTTPS (localhost est considéré comme sûr par les navigateurs)
JWT_SECURE_COOKIE=true
WEB_PORT=8090
